# Методология расчета APY за 30 дней для пулов Uniswap v3

## 📊 Обзор

Данный документ описывает три метода получения 30-дневного APY для пулов Uniswap v3:
1. **Dune Analytics** - SQL-запросы к историческим данным блокчейна
2. **The Graph** - GraphQL-запросы к индексированным данным Uniswap v3 subgraph
3. **Revert Finance** - Специализированная аналитическая платформа

**Дата анализа:** 25 января 2026
**Анализируемый период:** 30 дней
**Количество пулов:** 10

---

## 🎯 Общая методология расчета APY

Для всех трех методов используется единая формула расчета годовой процентной доходности (APY):

```
APY = (Комиссии_30д / TVL_средний) × (365 / 30) × 100%

Где:
- Комиссии_30д: Общие комиссии, собранные пулом за последние 30 дней (в USD)
- TVL_средний: Средний Total Value Locked за период (в USD)
- 365 / 30: Коэффициент аннуализации (пересчет на год)
```

### Альтернативная формула через объем торгов:

```
Комиссии_30д = Объем_торгов_30д × (Ставка_комиссии / 100)

APY = (Объем_торгов_30д × Ставка_комиссии / 100 / TVL_средний) × (365 / 30) × 100%
```

---

## 1️⃣ Метод 1: Dune Analytics

### 📋 Описание

Dune Analytics предоставляет доступ к историческим данным блокчейна через SQL-запросы. Это позволяет получить наиболее полные и достоверные данные напрямую из блокчейна.

### 🔑 Требования

- **API ключ:** Требуется регистрация на [dune.com](https://dune.com)
- **Python библиотека:** `pip install dune-client`
- **Создание запроса:** Необходимо создать SQL запрос в Dune UI и получить `query_id`

### 📝 SQL Запрос

```sql
WITH pool_daily_stats AS (
    SELECT
        pool AS pool_address,
        DATE_TRUNC('day', block_time) AS day,
        SUM(amount0 * p0.price + amount1 * p1.price) AS volume_usd,
        AVG(reserve0 * p0.price + reserve1 * p1.price) AS tvl_usd
    FROM uniswap_v3_ethereum.Pool_evt_Swap s
    LEFT JOIN prices.usd p0 ON p0.contract_address = s.token0
        AND DATE_TRUNC('day', p0.minute) = DATE_TRUNC('day', s.block_time)
    LEFT JOIN prices.usd p1 ON p1.contract_address = s.token1
        AND DATE_TRUNC('day', p1.minute) = DATE_TRUNC('day', s.block_time)
    WHERE pool IN ('<адреса_пулов>')
        AND block_time >= NOW() - INTERVAL '30' DAY
    GROUP BY 1, 2
),
pool_fees AS (
    SELECT
        pool_address,
        SUM(volume_usd * fee_tier / 1000000) AS total_fees_usd,
        AVG(tvl_usd) AS avg_tvl_usd,
        AVG(volume_usd) AS daily_volume_usd,
        COUNT(DISTINCT day) AS days_count
    FROM pool_daily_stats pds
    LEFT JOIN uniswap_v3_ethereum.Factory_evt_PoolCreated pc
        ON pc.pool = pds.pool_address
    GROUP BY 1
)
SELECT
    pool_address,
    total_fees_usd,
    avg_tvl_usd,
    daily_volume_usd,
    days_count,
    CASE
        WHEN avg_tvl_usd > 0 THEN (total_fees_usd / avg_tvl_usd) * (365.0 / 30.0) * 100
        ELSE 0
    END AS apy_30d
FROM pool_fees
ORDER BY apy_30d DESC
```

### ✅ Преимущества

- ✅ **Полнота данных:** Прямой доступ к блокчейну, все исторические данные
- ✅ **Гибкость:** Возможность создавать кастомные SQL запросы
- ✅ **Точность:** Данные берутся непосредственно из событий блокчейна
- ✅ **Дополнительная аналитика:** Можно получить любые метрики (транзакции, газ и т.д.)

### ⚠️ Недостатки

- ❌ **Сложность:** Требуется знание SQL и структуры данных Uniswap v3
- ❌ **API ключ:** Необходима регистрация и получение API ключа
- ❌ **Двухэтапность:** Нужно создать запрос в UI, затем вызвать через API
- ❌ **Лимиты:** Ограничения на количество запросов в зависимости от тарифа

### 📊 Получаемые метрики

- `pool_address` - адрес пула
- `total_fees_usd` - общие комиссии за 30 дней
- `avg_tvl_usd` - средний TVL за 30 дней
- `daily_volume_usd` - средний дневной объем торгов
- `days_count` - количество дней с данными
- `apy_30d` - рассчитанный APY

### 🔗 Ресурсы

- Документация API: https://docs.dune.com/api-reference/
- Примеры запросов: https://dune.com/projects/uniswap
- Python клиент: https://github.com/duneanalytics/dune-client

---

## 2️⃣ Метод 2: The Graph

### 📋 Описание

The Graph предоставляет децентрализованный доступ к индексированным данным блокчейна через GraphQL API. Uniswap v3 имеет официальный subgraph с готовыми структурами данных.

### 🔑 Требования

- **API ключ:** Создается в [The Graph Studio](https://thegraph.com/studio/)
- **Python библиотека:** `pip install requests`
- **Subgraph ID:** `5zvR82QoaXYFyDEKLZ9t6v9adgnptxYpKpSbxtgVENFV` (Ethereum Mainnet)

### 📝 GraphQL Запрос

```graphql
{
  pool(id: "0x<pool_address>") {
    id
    token0 {
      symbol
    }
    token1 {
      symbol
    }
    feeTier
    totalValueLockedUSD
    volumeUSD
    feesUSD
    poolDayData(
      first: 30
      orderBy: date
      orderDirection: desc
      where: { date_gte: <timestamp_30d_ago> }
    ) {
      date
      volumeUSD
      tvlUSD
      feesUSD
      txCount
    }
  }
}
```

### 🔄 Процесс получения данных

1. Для каждого пула выполняется отдельный GraphQL запрос
2. Получаются данные `poolDayData` за последние 30 дней
3. Суммируются комиссии: `total_fees = SUM(poolDayData[].feesUSD)`
4. Рассчитывается средний TVL: `avg_tvl = AVG(poolDayData[].tvlUSD)`
5. Вычисляется APY: `(total_fees / avg_tvl) × (365 / 30) × 100`

### ✅ Преимущества

- ✅ **Готовые структуры:** Данные уже индексированы и структурированы
- ✅ **Простота:** GraphQL проще, чем SQL для многих разработчиков
- ✅ **Скорость:** Быстрый доступ к данным без обработки сырых событий
- ✅ **Децентрализация:** The Graph - децентрализованная сеть
- ✅ **Официальный источник:** Uniswap поддерживает свой subgraph

### ⚠️ Недостатки

- ❌ **API ключ:** Требуется регистрация в The Graph Studio
- ❌ **Лимиты:** Ограничения на количество запросов и размер ответа
- ❌ **Задержка индексации:** Возможна небольшая задержка обновления данных
- ❌ **Ограниченная кастомизация:** Нельзя получить данные вне схемы subgraph

### 📊 Получаемые метрики

- `id` - адрес пула
- `token0`, `token1` - информация о токенах
- `feeTier` - ставка комиссии
- `totalValueLockedUSD` - текущий TVL
- `volumeUSD` - общий объем торгов
- `feesUSD` - общие комиссии
- `poolDayData[]` - массив дневных данных:
  - `date` - дата (unix timestamp)
  - `volumeUSD` - объем торгов за день
  - `tvlUSD` - TVL на конец дня
  - `feesUSD` - комиссии за день
  - `txCount` - количество транзакций

### 🔗 Ресурсы

- Документация Uniswap: https://docs.uniswap.org/api/subgraph/
- Примеры запросов: https://docs.uniswap.org/api/subgraph/guides/v3-examples
- The Graph Explorer: https://thegraph.com/explorer/
- GitHub репозиторий: https://github.com/Uniswap/v3-subgraph

---

## 3️⃣ Метод 3: Revert Finance

### 📋 Описание

Revert Finance - специализированная аналитическая платформа для провайдеров ликвидности на Uniswap v3, Sushiswap и других AMM протоколах. Предоставляет удобный веб-интерфейс с готовыми метриками.

### 🔑 Требования

- **Доступ:** Бесплатный веб-интерфейс на [revert.finance](https://revert.finance/)
- **API:** Публичный API отсутствует, данные собираются вручную

### 📝 Процесс сбора данных

1. Открыть страницу пула: `https://revert.finance/#/pool/1/{pool_address}`
2. Выбрать период "30d" в интерфейсе
3. Записать метрики:
   - **Total Fees** - общие комиссии за 30 дней (USD)
   - **Avg TVL** - средний TVL за период (USD)
   - **Volume 30d** - объем торгов за 30 дней (USD)
   - **Fee APR** - APR от комиссий (если отображается)
   - **APY** - если отображается напрямую

4. Если APY не показан, рассчитать вручную:
   ```
   APY = (Total_Fees_30d / Avg_TVL) × (365 / 30) × 100%
   ```

### ✅ Преимущества

- ✅ **Удобство:** Готовый интерфейс с визуализацией
- ✅ **Не требует кода:** Подходит для неразработчиков
- ✅ **Бесплатно:** Нет необходимости в API ключах
- ✅ **Дополнительная аналитика:** Графики, impermanent loss, backtesting
- ✅ **Специализация:** Инструменты специально для LP на Uniswap v3

### ⚠️ Недостатки

- ❌ **Ручной сбор:** Нет автоматизации, данные копируются вручную
- ❌ **Масштабируемость:** Неэффективно для большого количества пулов
- ❌ **Нет API:** Невозможно интегрировать в автоматические системы
- ❌ **Возможны ошибки:** Человеческий фактор при копировании данных

### 📊 Получаемые метрики

- **Pool Info:** Название пула, токены, ставка комиссии
- **Total Fees (30d):** Общие комиссии за 30 дней
- **TVL (Avg):** Средний TVL за период
- **Volume (30d):** Объем торгов за 30 дней
- **Transactions:** Количество транзакций
- **Fee APR/APY:** Доходность от комиссий
- **IL (Impermanent Loss):** Непостоянные потери (если применимо)

### 🔗 Ресурсы

- Главная страница: https://revert.finance/
- Pool Ranking: https://revert.finance/#/pool-ranking
- Документация: https://docs.revert.finance/
- GitHub: https://github.com/revert-finance

---

## 📊 Сравнительная таблица методов

| Критерий | Dune Analytics | The Graph | Revert Finance |
|----------|----------------|-----------|----------------|
| **Требуется API ключ** | ✅ Да | ✅ Да | ❌ Нет |
| **Автоматизация** | ✅ Полная | ✅ Полная | ❌ Ручной сбор |
| **Сложность внедрения** | 🔴 Высокая | 🟡 Средняя | 🟢 Низкая |
| **Полнота данных** | 🟢 Максимальная | 🟢 Высокая | 🟡 Базовая |
| **Скорость получения** | 🟡 Средняя | 🟢 Быстрая | 🔴 Медленная |
| **Гибкость запросов** | 🟢 Максимальная | 🟡 Ограниченная | 🔴 Нет |
| **Стоимость** | 🟡 Есть лимиты | 🟡 Есть лимиты | 🟢 Бесплатно |
| **Точность данных** | 🟢 Очень высокая | 🟢 Высокая | 🟢 Высокая |
| **Подходит для** | Разработчики | Разработчики | Все пользователи |

---

## 🎯 Рекомендации по выбору метода

### Для разработчиков и автоматизации:

1. **The Graph** - рекомендуется для большинства случаев:
   - Готовые структуры данных
   - Простой GraphQL API
   - Официальная поддержка Uniswap
   - Хороший баланс между простотой и функциональностью

2. **Dune Analytics** - для сложной аналитики:
   - Когда нужны кастомные метрики
   - Для глубокого анализа данных
   - Когда требуется максимальная гибкость SQL

### Для ручного анализа:

3. **Revert Finance** - для быстрого анализа:
   - Не требуется код
   - Удобный интерфейс
   - Дополнительные метрики (IL, backtesting)
   - Подходит для малого количества пулов

---

## 📈 Пример расчета APY

Рассмотрим пул **WETH / USDT 0.3%**:

### Исходные данные (гипотетические):
- Общие комиссии за 30 дней: $1,420,000
- Средний TVL за 30 дней: $110,000,000
- Ставка комиссии: 0.3%

### Расчет:
```
APY = (1,420,000 / 110,000,000) × (365 / 30) × 100%
APY = 0.01291 × 12.167 × 100%
APY = 15.71%
```

### Интерпретация:
При текущих условиях (объем торгов, TVL), провайдер ликвидности может ожидать годовую доходность около **15.71%** от комиссий, не учитывая:
- Непостоянные потери (Impermanent Loss)
- Комиссии сети (gas fees)
- Изменения в объеме торгов и TVL

---

## ⚠️ Важные ограничения

### 1. Историчность данных
- APY рассчитывается на основе прошлых 30 дней
- Прошлая доходность не гарантирует будущую
- Рынок криптовалют высоко волатилен

### 2. Не учтенные факторы
- **Impermanent Loss:** Потери от изменения цены токенов
- **Gas fees:** Стоимость транзакций в сети Ethereum
- **Управление позицией:** Затраты на ребалансировку в Uniswap v3
- **Изменения ликвидности:** APY меняется с изменением TVL

### 3. Технические ограничения
- **Dune:** Требует создания query_id, есть лимиты API
- **The Graph:** Может быть задержка индексации
- **Revert Finance:** Нет автоматизации, ручной труд

### 4. Качество данных
- Разные источники могут давать немного разные результаты
- Это нормально из-за:
  - Разного времени обновления данных
  - Разных методик расчета среднего TVL
  - Учета или не учета определенных типов транзакций

---

## 📝 Заключение

Все три метода позволяют получить достоверные данные о 30-дневном APY пулов Uniswap v3:

- **Dune Analytics** - для глубокого анализа и кастомных запросов
- **The Graph** - оптимальный выбор для автоматизированных систем
- **Revert Finance** - для быстрого ручного анализа

Рекомендуется использовать несколько методов для перекрестной проверки данных и получения более полной картины.

---

**Дата подготовки документа:** 25 января 2026
**Автор:** AI Issue Solver
**Версия:** 1.0
